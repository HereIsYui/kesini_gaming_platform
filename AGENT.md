# 项目本地约定

## 玩家端页面布局

- 新增顶部主导航页面时，根级 `panel` 必须复用现有页面内边距：桌面端 `20px`，移动端 `14px`。
- 不要给单页根容器额外加顶部外边距；页面顶部间距统一交给 `.page` 控制。
- 新增页面样式前先检查 `website/src/styles.css` 里的共享面板选择器，避免新页面漏掉统一 padding。

## UI 文案

- 按钮文字不超过 4 个字。
- 卡片说明不超过 15 个字。
- 不向玩家暴露技术词。
- 不写解释系统设计的长文案。
