{
  "name": "kesini-gaming-platform",
  "version": "1.0.5",
  "private": true,
  "workspaces": [
    "system",
    "backend",
    "website"
  ],
  "scripts": {
    "dev": "concurrently -k -n system,admin,web -c blue,magenta,green \"npm run system:dev\" \"npm run backend:dev\" \"npm run website:dev\"",
    "system:dev": "npm --workspace system run start:dev",
    "system:build": "npm --workspace system run build",
    "system:test": "npm --workspace system run test",
    "system:seed:card-pools": "npm --workspace system run seed:card-pools --",
    "backend:dev": "npm --workspace backend run dev",
    "backend:build": "npm --workspace backend run build",
    "backend:preview": "npm --workspace backend run preview",
    "website:dev": "npm --workspace website run dev",
    "website:build": "npm --workspace website run build",
    "website:preview": "npm --workspace website run preview",
    "build": "npm run system:build && npm run backend:build && npm run website:build",
    "test": "npm run system:test",
    "audit:critical": "npm audit --audit-level=critical"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
